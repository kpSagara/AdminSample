<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lpuadmin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lpuad"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lpuadr"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewsPopUp"></activity>
        <activity android:name=".NewsUpdate" />
        <activity android:name=".NewsImageActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".UpdateItem" />
        <activity android:name=".imagepopup" />
        <activity android:name=".EventImageActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ImagesActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>